# AI-Attempt
This is my first AI creation. It is a simple transformer using deterministic (not stochastic / probabilistic) memory, but can be configured depending on the response code.

The other files in the code are unused and no longer should exist, but if you want to use them, feel free. They just break since main.py is no longer the same (I need to learn branches)'

**REQUIREMENTS**:
* You will need to run the command: pip install torch OR pip3 install torch
