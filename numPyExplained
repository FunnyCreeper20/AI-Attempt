In short, the numPy version does not work.

This is because it only does the forward pass without any attention loops.
If we had attention loops for training, it would require a LOT of calculus

In pyTorch, the loss.backward() and optimizer.step() did about 1,000 lines of calculus automatically
To do that in NumPy, you would need to write the Chain Rule for every single matrix

Overall, it just reports a random word.
